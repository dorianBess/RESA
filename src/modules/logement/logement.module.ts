import { Module } from '@nestjs/common';

// Structure hexagonale identique à ReservationModule
// À implémenter : LogementController, LogementRepository, use-cases CRUD

@Module({})
export class LogementModule {}
