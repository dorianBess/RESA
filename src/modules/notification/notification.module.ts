import { Module } from '@nestjs/common';
// Port sortant : PORT_EMAIL (AWS SES)
// Use-cases : EnvoyerConfirmationReservationUseCase, EnvoyerAlerteConflitUseCase
@Module({})
export class NotificationModule {}
